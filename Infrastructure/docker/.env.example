# Copy this file to ".env" (same folder as docker-compose.yml) and set a
# real password before running `docker compose up`.
#
# SQL Server requires: at least 8 characters, and at least 3 of the
# following 4 categories — uppercase, lowercase, digit, symbol.

SA_PASSWORD=YourStr0ng!Passw0rd
